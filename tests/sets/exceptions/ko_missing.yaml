global-url: http://localhost:8000
global-timeout: 300

calls:
  - call:
      id : "Base url"
      expect : 200
      headers: 
        authorization : 'Bearer'
      method : GET
      timeout: 500
